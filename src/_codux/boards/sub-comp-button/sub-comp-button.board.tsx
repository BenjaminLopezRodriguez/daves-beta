import { createBoard } from '@wixc3/react-board';
import { SubCompButton } from '../../../components/sub-comp-button/sub-comp-button';

export default createBoard({
    name: 'SubCompButton',
    Board: () => <SubCompButton />
});
